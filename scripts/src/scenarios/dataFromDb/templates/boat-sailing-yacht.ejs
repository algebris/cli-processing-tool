<% if (isActualBoat) { %>
  The <%= boat %> powerboat
  <% if (isPlanning) { %>will be produced<% } else { %>is produced<% } %>
  by the brand <%= shipyard %>
<% } else { %>
  The <%= boat %> powerboat was produced by the brand <%= shipyard %>
<% } %>

<% if (yearFrom) { %>
  since <%= yearFrom %>
<% } %>

<% if (!isActualBoat && yearTo) { %>
  <% if (yearFrom) { %>to<% } else { %>until<% } %>
  <%= yearTo %>
<% } %>.

The <%= boat %> is a <%= loa %> meters <%= category %>
<% if (cabinsCount || guestsCount || draft) { %>
  with
  <% if (cabinsCount) { %>
    <%= cabinsCount + (cabinsCount == 1 ? ' guest cabin' : ' guest cabins') %>
  <% } else if (guestsCount) { %>
    accommodation for
    <% if (guestsCount == 1) { %>
    one person
    <% } else { %>
    <%= guestsCount %> people
    <% } %>
  <% } %>
  <% if (draft) { %>
    <% if (cabinsCount || guestsCount) { %> and a <% } else { %> a <% } %>
    draft of <%= draft %> meters
  <% } %>
<% } %>

<% if (maxSpeed) { %>
  which can reach speeds of up to <%= maxSpeed %> knots
<% } else if (cruiseSpeed) { %>
  which can cruise at <%= cruiseSpeed %> knots
<% } %>.

<% if (ceCertificate) { %>
  The boat with a CE certification class (<%= ceCertificate %>) and can navigate
  <% if (ceCertificate == 'A') { %>
    the open ocean
  <% } else if (ceCertificate == 'B') { %>
    no further than 200 miles off the coastline
  <% } else if (ceCertificate == 'C') { %>
    along the coastline and on rivers and canals
  <% } else if (ceCertificate == 'D') { %>
    in protected waters
  <% } %>
  .
<% } %>

<% if (isActualBoat && price) { %>
  The base price of a new <%= boat %> is <%= price %>.
<% } %>
